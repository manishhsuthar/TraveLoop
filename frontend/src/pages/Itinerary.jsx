export default function Itinerary() {
  return (
    <section>
      <h1>Itinerary Builder</h1>
      <p>Add stops, dates, and activities from backend endpoints (`/api/stops`, `/api/stop-activities`).</p>
    </section>
  );
}
