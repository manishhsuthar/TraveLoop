export default function Notes() {
  return (
    <section>
      <h1>Trip Notes / Journal</h1>
      <p>Use backend endpoint `/api/notes` to create day-wise reminders and journal entries.</p>
    </section>
  );
}
